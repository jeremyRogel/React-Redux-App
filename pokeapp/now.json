{
    "version": 2,
    "name": "pokeapp",
    "builds": [{ "src": "pokeapp/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "pokeapp/$1" }
    ]
}